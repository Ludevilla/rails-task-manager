<h1>My Tasks</h1>
<div>
  <ul>
    <% @tasks.each do |task| %>
    <li> <%= link_to task.title, task_path(task) %> </li>
    <% end %>
  </ul>
</div>
<div> <%= link_to "Add a new task", new_path %> </div>
